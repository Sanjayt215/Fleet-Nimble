# Performance Report

- Socket.IO: infinite reconnect with backoff (`socket.js`)
- Dashboard: caps live buffer at 20 points, vehicle list limit 8
- Diagnostics: history limited to 50 samples per request
- **Future:** React.lazy route splitting for `/admin`, `/reports`
