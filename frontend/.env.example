VITE_API_URL=http://localhost:5000/api
# Socket base URL (no trailing /api) — MUST be set on Vercel
VITE_SOCKET_URL=http://localhost:5000

# ── Production (Vercel) ──
# VITE_API_URL=https://fleet-nimble.onrender.com/api
# VITE_SOCKET_URL=https://fleet-nimble.onrender.com
#
# IMPORTANT: Both VITE_API_URL and VITE_SOCKET_URL must be set in Vercel dashboard.
# VITE_SOCKET_URL must NOT include /api suffix — Socket.IO lives at root.
